//Declaration of assertion and XMLParser requirements
var assert = require('chai');
var XMLParser = require('../../../REST_API/modules/XMLParser');

//Test Methods
describe('XMLParser', function() {
    describe('getData', function () {
        it('should return list of currency pairs along with their rates',function () {
            assert.typeOf(value, name, "[message]");
        })
        
    })
});